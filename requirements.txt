# ─────────────────────────────────────────────────────────────────────────────
# LipSight — core backend + CV runtime dependencies.
# These are enough to run the API, DB, video validation/metadata, frame
# sampling, quality scoring, exports, and the full test-suite (with mock ML).
# Heavy ML runtimes live in requirements-ml.txt (installed only when needed).
# ─────────────────────────────────────────────────────────────────────────────

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9
sqlalchemy>=2.0
numpy>=1.26
opencv-python-headless>=4.9
pillow>=10.0
pyyaml>=6.0

# optional DB drivers (install the one matching DATABASE_URL)
# psycopg[binary]>=3.1   # PostgreSQL
# aiosqlite>=0.19        # async SQLite

# testing
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
